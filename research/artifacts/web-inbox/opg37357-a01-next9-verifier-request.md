# NEXT9 verification request, not a receipt

verdict: candidate_only
status: NONTERMINAL_CHECKPOINT
Repository: vibemathing/problem-opg-37357-obstacle-number
Issue: #3
Target: obligation:opg37357-root
Base: 3e135c3af759b6471f21ae053474b5b1196921bf
Candidate prefix: research/artifacts/candidates/opg37357-a01-next9-canonical-v1/

Freeze and review four separate layers:
1. The50-class order9 catalog is flip-closed, all667 embedded transitions valid. Completeness depends on the cited Mori-Nakamoto-Ota spherical theorem; smaller-order exclusion is reused MIN4. No geometric placement is constrained by this catalog's rotations.
2. witness.json gives a34+5 filled-simple-polygon whole-graph representation. Reused byte-pinned NB9 polygon_verify.py checks every edge/nonedge directly, independently of construction logic, but not as a separately trusted principal. Justify Jordan/filled-region semantics and match the ProblemContract.
3. The cap lemma requires every stated local visibility/separation premise, especially CLOSED cap disjointness from all other obstacles. ear_check.py checks its concrete quadrilateral instance, the parent representation, point insertion and final graph. Do not generalize this to all structural edge expansions.
4. compact_verify.py is a lossless memory adapter for the earlier consumer. It reconstructs all121204 clauses/5000 paths and consumes one injective signed16 expected clause per actual source. Fixed supplied-incidence projection is not a geometry completeness receipt or all-placement theorem.

Replay from the new directory under external35CPU/40wall seconds,512MiB,one worker,5MiB stdout:
python -B replay.py polygon
python -B replay.py ear
python -B replay.py catalog
python -B replay.py audit
python -B replay.py interface
python -B replay.py mutations
python -B replay.py guard
python -B replay.py path
python -B replay.py omit
python -B replay.py flip

Full emitted caches: python -B replay.py cnf, origins-a, origins-b. Concatenate origin halves in that order. Exact output hashes and final source hashes are in execution.json. Old sibling NB9 dependencies must keep the digests in provenance.json; they are not copied into the new candidate.

The first tuple-set consumer and intermediate compact/output layouts exceeded512MiB. This did not justify weakening any formula or raising the gate. Final algorithm and all commands were actually rerun after lossless packing, expected-set consumption and streaming hashing. Raw diagnostics with runtime addresses or host paths are not published.

The fixture verifier registry provides no compatible authenticated geometry/compiler/closure invocation obtained in this turn. Requested capabilities are ONLY root-admitted kernel_check, axiom_escape_audit, statement_faithfulness. This request is not an EvidenceLink, signature, Result, Solution or trusted receipt. T (Jordan/polygons), A (incidence completeness), P (all-placement growth) remain separate. Root remains open.
